// Definimos una función llamada saludar
function saludar(nombre) {
  // Concatenamos el nombre con el saludo y lo mostramos en la consola
  console.log("¡Hola, " + nombre + "!");
}

// Llamamos a la función con un nombre específico
saludar("Juan");
