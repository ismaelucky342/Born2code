#include <stdio.h>

int main() {
    // Definición de variables
    int a = 10;
    int b = 3;

    // Suma
    int suma = a + b;
    printf("La suma es: %d\n", suma);

    // Resta
    int resta = a - b;
    printf("La resta es: %d\n", resta);

    // Multiplicación
    int multiplicacion = a * b;
    printf("La multiplicación es: %d\n", multiplicacion);

    // División
    float division = (float) a / b;
    printf("La división es: %.2f\n", division);

    return 0;
}
/*HEMOS INCORPORADO LAS 4 EXPRESIONES PARA VER COMO SE REALIZAN*/